version: "3"

services:

  monitor: 
    image: spring-kubernetes-monitor
    ports: 
      - "8001:8080"
  
  model-generator:
    image: spring-model-generator
    ports: 
      - "8002:8080"
  
  activemq:
    image: webcenter/activemq:latest
